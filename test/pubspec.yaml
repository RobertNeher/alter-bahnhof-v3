name: test
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0-279.3.beta <4.0.0'

dependencies:
  http:
  intl:
  settings:
    path: '../settings'
  utils:
    path: '../utils'
  model:
    path: '../model'
  alter_bahnhof_widgets:
    path: '../alter_bahnhof_widgets'
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
